spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test1
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8000/eureka
  instance:
    hostname: localhost

server:
  port: 9001
